/node_modules

.env
.env.*

ggeServiceKey.json
anotherGgeServiceKey.json

# Firebase service account (chứa private key — KHÔNG commit)
firbase.json
firebase.json
*serviceAccount*.json

# Tài khoản mẫu chứa mật khẩu dev — giữ local, không commit
ACCOUNTS.md

package-lock.json
pnpm-lock.yaml

# Claude Code local tooling config
.claude/
